//Easy way of initializing an array
const gameList = ['Dark Souls 3', 'Elden Ring', 'Pokemon Blue'];

//Another way of initializing an array
const games = new Array('Dark Souls 3', 'Elden Ring', 'Pokemon Blue')

//Selecting specific elements from an array
// console.log(gameList[0])

//changes the 2nd element inside the games array into 'Dota 2'
games[1] = 'Dota 2';

//arrays can contain different data types
const example1 = ['string', 100 - 20, true, {name: 'object', purpose: 'learning'}]
// console.log(example1)








//.length

//returns the length of the array, non zero based
//console.log(gameList.length)

//always returns the last element in the array
//console.log(gameList[gameList.length - 1])
